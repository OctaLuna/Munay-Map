# Variables de entorno para el frontend
# Copiar a .env.local para desarrollo local

# URL base del backend NestJS (actualmente no usada — USE_MOCK_DATA=true en featureFlags.ts)
# Cuando el backend esté listo: cambiar USE_MOCK_DATA a false y configurar esta URL
VITE_API_BASE_URL=http://localhost:3000/api
